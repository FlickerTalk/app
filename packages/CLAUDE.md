# app/packages/

Paquetes TypeScript de la app (`Plan.md §81`): de momento solo `ui/`. El contrato de plugins vive
en su propio repo (`plugin-sdk/`, MIT). Mismas reglas de frontend que la app: Lit + Web
Components + TypeScript, sin React/React Native/Angular (`§83`), e interfaz con iconos primero;
el texto imprescindible va en inglés, sin traducciones en la fase 1 (`§84`).
