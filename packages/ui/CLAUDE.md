# app/packages/ui

Componentes de UI reutilizables (Vue 3 + Ionic + TypeScript).

El plan (`§81`, `§83`) solo fija el nombre y el stack. El reparto de componentes entre este
paquete y `app/src` se decide al construir la UI (fase 3, `§92`).
