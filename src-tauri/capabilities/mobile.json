{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Phone-only permissions: the camera scanner for Contact Card QR codes (Plan §32)",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["barcode-scanner:default"]
}
