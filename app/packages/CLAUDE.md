# app/packages/

Paquetes TypeScript compartidos (`Plan.md §81`): `ui/` y `plugin-sdk/`. Mismas reglas de
frontend que la app: Lit + Web Components + TypeScript, sin React/React Native/Angular (`§83`),
e interfaz con iconos primero; el texto imprescindible va en inglés, sin traducciones en la
fase 1 (`§84`).
